# Fail-hidden visitor Presence after track/list failure

**Category:** bug
**Status:** done
**Source:** `.claude/plans/donor-visitor-presence/reviews/donor-visitor-presence-review.md`

## Summary

Donor visitor Presence must stay fail-hidden: tracking or listing failures must leave the donor form usable and the visitor indicator hidden, including after later Presence diffs. Current coverage never exercises a real `Presence.track/4` error, and a returned track error leaves the LiveView subscribed so subsequent diffs can still show the indicator.

## Comments

### Triage — 2026-07-19

> *This was generated by AI during triage.*

Confirmed against current code: subscribe runs before track; the track error path logs and returns without unsubscribing or marking tracking inactive; matching `presence_diff` handlers always re-list and assign the topic count. The existing failure test omits `visitor_id` and only hits the local `:missing_visitor_id` fallback. Tracker process exits on track/list are also unhandled and can terminate the optional donor feature.

Moved to `ready-for-agent` with the brief below.

### Work — 2026-07-19

> *This was generated by AI during triage.*

Implemented durable fail-hidden Presence: `:visitor_tracking_active` gates diffs; failures unsubscribe and reset count; narrow `:exit` catch around track/list/untrack; `:visitor_presence` Application env seam for deterministic stubs. Verification: `mix test` 165 tests, 0 failures.

## Agent Brief

**Category:** bug
**Summary:** Make donor visitor Presence durable fail-hidden after track/list failures, including real tracker errors and exits

**Acceptance criteria:**
- [x] A deterministic test forces `Presence.track/4` to return an error (not merely missing `visitor_id`); the donor form remains usable and the indicator stays hidden
- [x] After that track failure, three successful presences join the same isolated topic; the failed view still shows the donor form and does not render the visitor indicator
- [x] A deterministic test covering a tracker exit during track and/or list leaves the donor form usable, resets/keeps count at zero, hides the indicator, and disables further Presence refreshes
- [x] Successful tracking path still shows the threshold indicator when unique browser-session count is >= 3
- [x] Focused Presence/donor tests pass; no regression to donor submit/payment flow
